export type DocumentConfig = {
  containerWidth: any;
  containerHeight?: any;
  editable?: boolean;
  flag?: string;
};

export type ShowDocumentConfig = {
  showOrginal: boolean;
  viewPercent: number;
};
