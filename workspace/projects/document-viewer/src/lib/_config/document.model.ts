export type DocumentConfig = {
  containerWidth: any;
  containerHeight?: any;
  editable?: boolean;
  flag?: string;
  debugMode?: boolean;
};

export type ShowDocumentConfig = {
  showOrginal: boolean;
  viewPercent: number;
};
