export type DocumentConfig = {
  containerWidth: any;
  containerHeight?: any;
  editable?: boolean;
  flag?: string;
  debugMode?: boolean;
  showCanvas?: boolean;
  docColor?: string;
};

export type ShowDocumentConfig = {
  showOrginal: boolean;
  viewPercent: number;
};
